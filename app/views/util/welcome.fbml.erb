<!-- None of the below will be shown, because it has been redirected by the
Controller. -->

welcome to PorchBBS, user:
    <fb:profile-pic uid="<%= @user %>"/>
    <fb:name uid="<%= @user %>"/>

from:
    <fb:profile-pic uid="<%= @from %>"/>
    <fb:name uid="<%= @from %>"/>
